# Mapbox (for sites map component)
NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_token

# GitHub OAuth (for feedback system)
# Create an OAuth App at: https://github.com/settings/developers
# Homepage URL: http://localhost:3000 (dev) or https://design.sourceful.energy (prod)
# Callback URL: http://localhost:3000/api/auth/callback/github (dev) or https://design.sourceful.energy/api/auth/callback/github (prod)
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# NextAuth secret (generate with: openssl rand -base64 32)
AUTH_SECRET=your_auth_secret_here
